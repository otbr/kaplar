#include "server.h"

struct protocol protocol_old_login = {
	.name = "old login",
	.identifier = 0xA0,
	.flags = 0,

	//
};

struct protocol protocol_old_game = {
	.name = "old game",
	.identifier = 0xA0,
	.flags = 0,

	//
};
